<p>
	<%- if controller_name != 'sessions' %>
	  <%= link_to "Log in", new_session_path(resource_name), class: "btn btn-danger another" %> <br />
	<% end -%>

</p>


<p>
	<%- if devise_mapping.registerable? && controller_name != 'registrations' %>

	  <%= link_to "Register As Dealership", new_new_dealer_contact_path, class: "btn btn-danger" %>
	  <%= link_to "Register As Individual Customer", new_registration_path(resource_name), class: "btn btn-info" %>
	  <%= link_to "Register As Repairshop", new_new_dealer_contact_path, class: "btn btn-primary" %>
	  
	<% end -%>

</p>

